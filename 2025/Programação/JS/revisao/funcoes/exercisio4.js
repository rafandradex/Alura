const calcularMedia = (nota1, nota2) => {
    const notaFinal = (nota1 + nota2) / 2
    console.log(notaFinal);
    
}

//const calcularMedia = (nota1, nota2) => (nota1 + nota2) / 2; Opção mais facil

calcularMedia(10, 20)