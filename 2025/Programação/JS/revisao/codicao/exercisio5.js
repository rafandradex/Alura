const usuarioo = "admin";

if (usuarioo === "admin"){
    console.log('Login bem-sucedido!');
} else {
    console.log('Usuário inválido.');
}

// Alura

const usuario = "admin";

const mensagem = (usuario === "admin") ? "Login bem-sucedido!" : "Usuário inválido.";

console.log(mensagem);